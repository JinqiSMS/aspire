experiment_name: independent_smoke
kind: single_layer
oracle:
  mode: true_suffix_debug
recovery:
  moment_samples_by_layer: [128]
sampler:
  mode: independent_endpoints
  endpoint_steps: 64
diagnostic:
  reference_directions: 2048
