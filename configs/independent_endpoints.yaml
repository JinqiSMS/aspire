# Experimental choices; not theorem-certified constants.
experiment_name: independent_endpoints
sampler:
  mode: independent_endpoints
oracle:
  max_real_learning_queries: 6000000
evaluation:
  gaussian_test_points: 1000
  gaussian_batches: 2
  angular_risk_directions: 1000
  angular_risk_batches: 2
